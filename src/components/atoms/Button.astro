---
// Button as an Atom
interface Props {
  label: string;
  onClick?: () => void;
}

const { label } = Astro.props;
---
<button
  class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600"
>
  {label}
</button>
